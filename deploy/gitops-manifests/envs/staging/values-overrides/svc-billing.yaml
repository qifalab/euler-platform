# staging overrides for svc-billing (values merge on top of the chart defaults).
image:
  tag: v0.1.0-rc
replicas: 2
# M-6 dual-AZ topology (00§4.4 P2): replicas hard-spread across AZs so losing
# one AZ does not take the service down. Staging mirrors prod topology.
topology:
  zoneSpread: true
