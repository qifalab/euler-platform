# Helm chart for alert-center — phase-3 alert convergence service (D-1) (08-devops §4.3).
# Syncs with ArgoCD sync-wave 10 (business services apply after middleware/gateway).

apiVersion: v2
name: alert-center
description: alert-center convergence/customer-notification service (03§4.4.6)
type: application
version: 0.1.0
appVersion: "0.1.0"
