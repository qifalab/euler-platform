# Kustomize entry for the APISIX gateway stack.
#
# The apisix-config and apisix-eu-plugins ConfigMaps are GENERATED from the
# real source files (config/config.yaml, plugins/*.lua) so the mounted config
# can never drift from the reviewed sources. Name-suffix hashing is disabled
# because the Deployments reference the ConfigMaps by fixed name.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mw-gateway
resources:
  - apisix-deployment.yaml
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
configMapGenerator:
  - name: apisix-config
    files:
      - config.yaml=config/config.yaml
  - name: apisix-eu-plugins
    files:
      - eu-auth.lua=plugins/eu-auth.lua
      - eu-authorize.lua=plugins/eu-authorize.lua
      - eu-audit.lua=plugins/eu-audit.lua
