# CODEOWNERS — gitops-manifests repo (08-devops §4.3)
#
# prod/ merges require SRE + service Owner dual approval; nobody (incl. admins)
# may bypass MR to push protected branches (08§4.2). Non-prod uses single review.

# Default: staging/dev single review by the platform team
*                                @qifalab/sre-platform

# Production environment overrides — dual approval required
/envs/prod/                      @qifalab/sre-platform @qifalab/owners-services

# Platform components (gateway/middleware/observability)
/platform/                       @qifalab/sre-platform @qifalab/infra-leads

# Admission & sync policies
/policies/                       @qifalab/architecture-committee
