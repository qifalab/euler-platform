{
  "name": "site",
  "version": "0.1.0",
  "private": true,
  "description": "StarCloud marketing portal — Nuxt3 SSR (02§8.1).",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --port 3000",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview --port 3000",
    "typecheck": "nuxt typecheck"
  },
  "dependencies": {
    "@sc/tokens": "workspace:*",
    "nuxt": "^3.13.0",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  }
}
