{
  "name": "docs-site",
  "version": "0.1.0",
  "private": true,
  "description": "StarCloud docs site — VitePress SSG (02§8.2).",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev --port 4000",
    "build": "vitepress build",
    "preview": "vitepress preview --port 4000"
  },
  "devDependencies": {
    "vitepress": "^1.4.0"
  }
}
