// StarCloud docs — custom theme entry (Google Cloud Glass brand overrides).
// Extends the VitePress default theme; custom.css layers brand tokens on top.
import DefaultTheme from "vitepress/theme";
import "./custom.css";

export default DefaultTheme;
