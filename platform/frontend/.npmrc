# Euler frontend — pnpm settings
#
# NOTE: pnpm 11 no longer reads linker/layout keys (node-linker, shamefully-hoist,
# overrides, allowBuilds, ...) from this file. Those now live in pnpm-workspace.yaml.
# This file is kept only for settings pnpm still honors here. See pnpm-workspace.yaml
# for the nodeLinker=hoisted rationale (Windows install deadlock workaround).
