version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    # FILE level (strictest): also catches deleted rpcs/services/files and
    # package moves, which WIRE_JSON would let through.
    - FILE
